name: Deploy static mirror to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Strip server-only surfaces (incompatible with static export)
        run: |
          rm -f src/middleware.ts
          rm -rf src/app/api

      - name: Build static export
        env:
          BUILD_TARGET: gh-pages
        run: npm run build

      - name: Add Pages artifacts (root redirect + .nojekyll)
        run: |
          touch out/.nojekyll
          cat > out/index.html <<'EOF'
          <!DOCTYPE html>
          <html lang="en">
            <head>
              <meta charset="utf-8" />
              <meta http-equiv="refresh" content="0; url=./en/" />
              <title>AEGIS Research</title>
            </head>
            <body>
              <p>Redirecting to <a href="./en/">AEGIS Research</a>…</p>
            </body>
          </html>
          EOF

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
